{
  "debug_mode": {
    "description": "调试模式",
    "type": "bool",
    "default": false,
    "hint": "开启后输出插件指令、缓存和联网请求的调试日志。Cookie、Token、密码等敏感字段会自动脱敏。"
  },
  "proxy_enabled": {
    "description": "启用网络代理",
    "type": "bool",
    "default": false,
    "hint": "开启后插件的全部联网请求通过下方 HTTP 代理访问。关闭时保持原有直连行为。"
  },
  "proxy_host": {
    "description": "代理地址",
    "type": "string",
    "default": "",
    "hint": "填写 HTTP 代理的 IP 或主机名，例如 127.0.0.1；不要填写 http://、端口或路径。"
  },
  "proxy_port": {
    "description": "代理端口",
    "type": "int",
    "default": 0,
    "hint": "填写 HTTP 代理端口，范围为 1 到 65535。"
  },
  "proxy_username": {
    "description": "代理用户名",
    "type": "string",
    "default": "",
    "hint": "可选。需要认证的代理请与代理密码同时填写。"
  },
  "proxy_password": {
    "description": "代理密码",
    "type": "string",
    "default": "",
    "hint": "可选。需要认证的代理请与代理用户名同时填写；插件日志不会输出该值。"
  },
  "use_global_calendar": {
    "description": "默认使用国际服日历",
    "type": "bool",
    "default": false,
    "hint": "关闭时默认查询国服日历；开启后默认查询国际服日历。也可以用“日历 国服”或“日历 国际服”临时指定。"
  },
  "weibo_cookie": {
    "description": "微博 Cookie",
    "type": "string",
    "default": "",
    "hint": "可选。用于“看看微博”命令请求微博移动端接口；不填写也会尝试公开访问，但微博可能返回 432 或限流。"
  },
  "fflogs_client_id": {
    "description": "FFLogs API Client ID",
    "type": "string",
    "default": "",
    "hint": "可选。用于“输出”命令在本地 boss 映射查不到时动态查询 FFLogs metadata，并用于“logs”命令查询角色战绩。"
  },
  "fflogs_client_secret": {
    "description": "FFLogs API Client Secret",
    "type": "string",
    "default": "",
    "hint": "可选。用于“输出”和“logs”命令获取 FFLogs OAuth token。请不要公开分享该字段。"
  },
  "use_global_fflogs": {
    "description": "默认使用国际服 FFLogs",
    "type": "bool",
    "default": false,
    "hint": "关闭时“输出”和“logs”默认查询国服 FFLogs；开启后默认查询国际服。也可以在命令末尾加“国服”或“国际服”临时指定。"
  },
  "font_path": {
    "description": "文本转图片字体路径",
    "type": "string",
    "default": "",
    "hint": "可选。用于价格、物品、房子、抽卡等文本转图片功能。推荐使用 SimHei.ttf；留空时会自动尝试使用 Linux 系统自带中文字体，如 Noto Sans CJK、文泉驿等。"
  },
  "ffxiv_icon_font_path": {
    "description": "FFXIV 招募板特殊图标字体路径",
    "type": "string",
    "default": "",
    "hint": "可选。用于“招募”卡片渲染游戏内特殊图标字符（U+E020-U+E0DB）。建议下载 FFXIV_Lodestone_SSF.ttf 到本地后填写绝对路径。"
  },
  "risingstones_checkin_hour": {
    "description": "石之家自动签到时点",
    "type": "int",
    "default": 8,
    "hint": "仅对已在私聊中绑定账号且手动开启自动签到的用户生效，使用 Asia/Shanghai 时区。范围为 0 到 23。"
  }
}
